import { IocContract } from '../Contracts';
export declare class Registrar {
    ioc: IocContract;
    private _basePath?;
    private _providersPaths;
    private _providers;
    private _collected;
    constructor(ioc: IocContract, _basePath?: string | undefined);
    private _loadProvider;
    private _collect;
    useProviders(providersPaths: string[]): this;
    register(): any[];
    boot(): Promise<void>;
    registerAndBoot(): Promise<any[]>;
}
