"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
function listDirectoryFiles(location, filterFn) {
    return fs_1.readdirSync(location, { withFileTypes: true }).filter((stat) => {
        if (stat.isDirectory()) {
            return false;
        }
        if (!stat.name.endsWith('.js')) {
            return false;
        }
        if (typeof (filterFn) === 'function') {
            return filterFn(stat);
        }
        return true;
    }).map((stat) => {
        return `./${stat.name}`;
    });
}
exports.listDirectoryFiles = listDirectoryFiles;
