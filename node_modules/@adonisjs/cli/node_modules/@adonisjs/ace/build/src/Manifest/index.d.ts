import { ManifestNode, CommandConstructorContract } from '../Contracts';
export declare class Manifest {
    private _basePath;
    constructor(_basePath: string);
    private _writeManifest;
    loadCommand(commandPath: string): CommandConstructorContract;
    lookupCommands(commandPath: string): CommandConstructorContract[];
    generate(commandPaths: string[]): Promise<void>;
    load(): Promise<ManifestNode>;
}
