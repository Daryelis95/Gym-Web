import { lines } from 'mrm-core';
import { BaseFile } from '../base/BaseFile';
export declare class LinesFile extends BaseFile {
    filePointer: ReturnType<typeof lines>;
    protected $actions: never[];
    constructor(basePath: string, filename: string);
    add(lines: string | string[]): this;
    update(oldText: string, newText: string): this;
    remove(lines: string | string[]): this;
    delete(): this;
    get(): string[];
    exists(): boolean;
    commit(): void;
    rollback(): void;
}
