import { JsonFile } from '../formats/JsonFile';
export declare class RcFile extends JsonFile {
    private _preloads;
    private _metaFiles;
    private _commands;
    constructor(basePath: string);
    onset(lifecycle: string, body: any): true | undefined;
    setExceptionHandler(namespace: string): this;
    setPreload(filePath: string, environment?: ('console' | 'test' | 'web')[], optional?: boolean): this;
    setAutoload(namespace: string, autoloadPath: string): this;
    setDirectory(key: string, value: string): this;
    addMetaFile(filePath: string, reloadServer?: boolean): void;
    addCommand(commandPath: string): void;
}
